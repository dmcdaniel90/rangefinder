// Default radius
export const defaultRadius: number = 25;

// Set up the default coordinates
export const defaultCoordinates: google.maps.LatLngLiteral = {
  lat: 51.5072178,
  lng: -0.1275862,
};

// Default location
export const defaultLocation: string = 'London, UK';
